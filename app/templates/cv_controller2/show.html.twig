{% extends 'base.html.twig' %}

{% block title %}Cv{% endblock %}

{% block body %}
    <h1>Cv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cv.id }}</td>
            </tr>
            <tr>
                <th>HeaderTitle</th>
                <td>{{ cv.headerTitle }}</td>
            </tr>
            <tr>
                <th>HeaderSkills</th>
                <td>{{ cv.headerSkills }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ cv.fileName }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ cv.logo }}</td>
            </tr>
            <tr>
                <th>NameSociety</th>
                <td>{{ cv.nameSociety }}</td>
            </tr>
            <tr>
                <th>CommercialInformation</th>
                <td>{{ cv.commercialInformation }}</td>
            </tr>
            <tr>
                <th>WebsiteSociety</th>
                <td>{{ cv.websiteSociety }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ cv.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cv_controller2_index') }}">back to list</a>

    <a href="{{ path('app_cv_controller2_edit', {'id': cv.id}) }}">edit</a>

    {{ include('cv_controller2/_delete_form.html.twig') }}
{% endblock %}
